# Production Environment Variables
# Loaded during 'npm run build' and 'next start'

# Leave empty to use domain-agnostic relative paths (e.g. /api/v1/...) 
# which Nginx will automatically route to the Django backend.
# If your backend is hosted on a completely separate domain, specify it here.
NEXT_PUBLIC_API_ENDPOINT=
